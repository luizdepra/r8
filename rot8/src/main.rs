use rotlib::machine::Machine;

const SCREEN_WIDTH: u32 = 64;
const SCREEN_HEIGHT: u32 = 32;
const SCREEN_SCALE: u32 = 1;

struct Interpreter {
    machine: Machine,
}

fn main() {
    println!("Hello, world!");
}
